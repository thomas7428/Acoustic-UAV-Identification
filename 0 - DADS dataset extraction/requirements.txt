# Dependencies for DADS dataset extraction
# Install with: pip install -r requirements.txt

# Core dataset libraries
datasets>=2.14.0
huggingface-hub>=0.16.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
audioread>=3.0.0

# Progress bar
tqdm>=4.65.0

# Scientific computing (if not already installed)
numpy>=1.19.5
scipy>=1.5.0
